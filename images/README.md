# Images

Les images utilisees par la brochure et le catalogue sont stockees dans `bateauvoyageur/`.

Convention recommandee : `bateau-ID.jpg`, par exemple `bateau-1.jpg` pour le bateau dont l'identifiant vaut `1`.

Les chemins sont resolus localement afin que FPDF puisse integrer les images dans le PDF.
