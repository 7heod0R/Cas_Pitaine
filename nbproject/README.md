# NetBeans

Ce dossier contient les fichiers de configuration du projet NetBeans (`project.xml`, `project.properties` et les reglages prives).

Ces fichiers ne participent pas directement au fonctionnement MVC sous Apache. Ils facilitent l'ouverture et l'execution du projet dans NetBeans.
