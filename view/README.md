# Vues

Ce dossier contient la presentation HTML/CSS de l'application.

- `accueil.php` : catalogue graphique des bateaux et bouton unique de generation PDF.

La vue recoit les variables preparees par `AccueilController.php`. Elle ne doit pas ouvrir de connexion PDO ni executer de requete SQL.
